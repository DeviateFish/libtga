/* libtga main */

// Base function.
var libtga = function() {
  // Add functionality here.
  return true;
};


// Version.
libtga.VERSION = '0.0.0';


// Export to the root, which is probably `window`.
root.libtga = libtga;
